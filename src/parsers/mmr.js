class MMRParser {
  /**
  * Parses the valorant player mmr
  * @param data {object} Data to parse
  * @returns {object} parsed data
  */
  constructor(data) {
    this.data = data;
  };

  parse() {
    
  //code here lol
  }

};
module.exports = MMRParser;
